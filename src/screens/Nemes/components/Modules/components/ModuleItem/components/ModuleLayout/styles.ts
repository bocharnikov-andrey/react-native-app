import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  scrollViewContainer: {
    flexDirection: "row",
    gap: 16,
  },
  smallTilesStackContainer: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 16,
  },
});
